<Window x:Class="BassesModManager.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BassesModManager"
        Title="Axon" Height="520" Width="832" MinHeight="480" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="Black"
        FontSize="15">
    <Grid>
    <Grid Margin="20">
        <Grid.Resources>
            <Style TargetType="ToolTip">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="#222"/>
                <Setter Property="FontSize" Value="15"/>
            </Style>
            <!-- One row: name + explanation on the left, switch on the right -->
            <Style x:Key="SettingNameStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Style>
            <Style x:Key="SettingHintStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#888"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="Margin" Value="0,3,0,0"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- 0: back -->
            <RowDefinition Height="Auto"/>  <!-- 1: heading -->
            <RowDefinition Height="Auto"/>  <!-- 2: settings -->
            <RowDefinition Height="*"/>     <!-- 3: spacer -->
            <RowDefinition Height="Auto"/>  <!-- 4: version label -->
            <RowDefinition Height="Auto"/>  <!-- 5: update notification bar -->
        </Grid.RowDefinitions>

        <!-- Same slot/size as the BACK button in MainWindow, so it doesn't jump when
             navigating between the two -->
        <Button Grid.Row="0" x:Name="BackButton"
                Content="BACK"
                Padding="12,0" Height="32"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Click="BackButton_Click"
                Style="{StaticResource PurpleButtonStyle}"
                ToolTip="Back"/>

        <TextBlock Grid.Row="1"
                   Text="SETTINGS"
                   Style="{StaticResource HeadingTextStyle}"
                   FontSize="26"
                   Margin="0,20,0,0"/>

        <!-- Capped width so each switch stays visually tied to its label instead of
             drifting to the far edge of the window -->
        <!-- IsSharedSizeScope + SharedSizeGroup below: each row is a separate Grid, so
             without this, each one's Auto column sizes to only its own control's width
             and the switches/buttons end up starting at different X positions instead of
             lining up in a column. -->
        <StackPanel Grid.Row="2" MaxWidth="620" HorizontalAlignment="Left" Margin="0,22,0,0"
                    Grid.IsSharedSizeScope="True">
            <Grid Margin="0,0,0,22">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="SettingControl"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="Sound effects" Style="{StaticResource SettingNameStyle}"/>
                    <TextBlock Text="Hover and click sounds in the app" Style="{StaticResource SettingHintStyle}"/>
                </StackPanel>
                <local:SettingToggle x:Name="SoundToggle" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center"
                                     Toggled="SoundToggle_Toggled"/>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="SettingControl"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="Show app after the game closes" Style="{StaticResource SettingNameStyle}"/>
                    <TextBlock Text="Off keeps the app minimized when you quit the game" Style="{StaticResource SettingHintStyle}"/>
                </StackPanel>
                <local:SettingToggle x:Name="RestoreToggle" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center"
                                     Toggled="RestoreToggle_Toggled"/>
            </Grid>

            <Grid Margin="0,22,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="SettingControl"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="Game executable" Style="{StaticResource SettingNameStyle}"/>
                    <TextBlock x:Name="GamePathText" Style="{StaticResource SettingHintStyle}"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="ChangeGameButton" Content="CHANGE"
                        HorizontalAlignment="Left"
                        Height="34" Padding="16,0"
                        Style="{StaticResource PurpleButtonStyle}"
                        Click="ChangeGameButton_Click"
                        ToolTip="Browse for your Star Wars Battlefront game folder"/>
            </Grid>

            <Grid Margin="0,22,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="SettingControl"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="Mod data folder" Style="{StaticResource SettingNameStyle}"/>
                    <TextBlock Text="Where applied mod combinations are stored" Style="{StaticResource SettingHintStyle}"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="OpenModDataButton" Content="OPEN"
                        HorizontalAlignment="Left"
                        Height="34" Padding="16,0"
                        Style="{StaticResource PurpleButtonStyle}"
                        Click="OpenModDataButton_Click"
                        ToolTip="Open the ModData folder in File Explorer"/>
            </Grid>
        </StackPanel>

        <!-- Version label -->
        <TextBlock Grid.Row="4" Text="v1.6" FontSize="14" Foreground="White" FontWeight="Bold"
                   VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="0,10,0,0"/>

        <!-- Update ready notification (hidden until UpdateService has a verified download) -->
        <local:UpdateNotificationBar Grid.Row="5" Margin="0,10,0,0"/>
    </Grid>
    <local:CustomCursorOverlay/>
    </Grid>
</Window>
