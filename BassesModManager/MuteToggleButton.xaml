<UserControl x:Class="BassesModManager.MuteToggleButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Button x:Name="ToggleButton" Padding="5,0" Height="30"
            Style="{StaticResource PurpleButtonStyle}"
            Click="ToggleButton_Click">
        <Viewbox Width="22" Height="22">
            <Canvas Width="24" Height="24">
                <!-- Speaker body - always visible -->
                <Path Data="M3,9 L7,9 L11,5 L11,19 L7,15 L3,15 Z" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                <!-- Sound waves - visible when not muted -->
                <Path x:Name="WavesIcon"
                      Data="M14,8 A5,5 0 0,1 14,16 M16.5,5.5 A8,8 0 0,1 16.5,18.5"
                      Stroke="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                      StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                <!-- Mute X - visible when muted -->
                <Path x:Name="MuteIcon" Visibility="Collapsed"
                      Data="M15,8 L21,16 M21,8 L15,16"
                      Stroke="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                      StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
            </Canvas>
        </Viewbox>
    </Button>
</UserControl>
