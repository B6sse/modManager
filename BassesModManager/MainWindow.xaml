<Window x:Class="BassesModManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:BassesModManager.Converters"
        Title="Basse's Mod Manager" Height="500" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="Black"
        FontSize="15">
    <Grid Margin="10">
        <Grid.Resources>
            <converters:StringToImageSourceConverter x:Key="StringToImageSourceConverter"/>
            <!-- Button style for hover effect -->
            <Style TargetType="Button" x:Key="PurpleButtonStyle">
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="Background" Value="#8000FF"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#8000FF"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Padding" Value="16,0"/>
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="#8000FF"/>
                        <Setter Property="BorderBrush" Value="White"/>
                        <Setter Property="Padding" Value="16,0"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <!-- RadioButton with white dot -->
            <Style TargetType="RadioButton" x:Key="WhiteDotRadioButton">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="Black"/>
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <StackPanel Orientation="Horizontal">
                                <Grid Width="18" Height="18" Margin="0,0,8,0">
                                    <Ellipse Stroke="White" StrokeThickness="2" Fill="Black"/>
                                    <Ellipse x:Name="Dot" Width="10" Height="10" Fill="White" Visibility="Collapsed"/>
                                </Grid>
                                <ContentPresenter VerticalAlignment="Center" />
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!-- Tooltip style -->
            <Style TargetType="ToolTip">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="#222"/>
                <Setter Property="FontSize" Value="15"/>
            </Style>
            <!-- Mod card: image with hover border -->
            <Style TargetType="RadioButton" x:Key="ModCardStyle">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="border" CornerRadius="12" BorderThickness="4" BorderBrush="Transparent" Margin="8" Padding="0">
                                <Grid Width="120" Height="120">
                                    <Grid.Clip>
                                        <RectangleGeometry RadiusX="12" RadiusY="12" Rect="0,0,120,120"/>
                                    </Grid.Clip>
                                    <Image Source="{Binding ImagePath, Converter={StaticResource StringToImageSourceConverter}}"
                                           Width="120" Height="120" Stretch="Uniform"
                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#8000FF"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#8000FF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Game Path Selection -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Select Game Path" Margin="0,0,0,5" Foreground="White" FontWeight="Bold" FontSize="15"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" CornerRadius="3" BorderBrush="White" BorderThickness="1">
                    <TextBox x:Name="GamePathTextBox" IsReadOnly="True" Foreground="White" Background="#222" Padding="5"/>
                </Border>
                <Button x:Name="BrowseButton" Grid.Column="1" Content="Browse" Margin="15,0,0,0" Width="70" Padding="5,5,5,5"
                        Style="{StaticResource PurpleButtonStyle}"
                        ToolTip="Browse for the game executable"
                        Click="BrowseButton_Click"/>
            </Grid>
        </StackPanel>

        <!-- Mod Selection Title (centered to align with centered image cards below) -->
        <TextBlock Grid.Row="1" Text="Select mod" Foreground="White" FontWeight="Bold" FontSize="15" HorizontalAlignment="Center" Margin="0,0,0,0"/>
        <!-- Mod Selection: centered image cards -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,8,0,0">
            <ItemsControl x:Name="ModListControl" HorizontalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <RadioButton GroupName="ModsGroup"
                                         IsChecked="{Binding IsEnabled}"
                                         Style="{StaticResource ModCardStyle}"
                                         ToolTip="{Binding Name}"/>
                            <TextBlock Text="{Binding Name}" Foreground="White" FontSize="13" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Launch Button -->
        <Button Grid.Row="5"
                x:Name="LaunchGameButton"
                Content="Launch Game"
                Height="40"
                Margin="0,10,0,0"
                Click="LaunchGameButton_Click"
                Style="{StaticResource PurpleButtonStyle}"
                ToolTip="Select a mod before launching the game"/>

        <!-- Version label -->
        <TextBlock Grid.Row="6" Text="Version 1.1" Foreground="White" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="0,10,0,0"/>
    </Grid>
</Window> 